{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'cd backend && npx prisma generate && npx prisma migrate resolve --rolled-back 20260410100000_add_meta_origen_enum; npx prisma migrate deploy && node src/index.js'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
